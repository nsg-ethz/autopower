# Autopower python API wrapper library

Includes an (AI generated) wrapper for querying the autopower server via grpc to start/stop measurements

Run with e.g.
```
python3 -m autopowermgmt.cli --config libs/autopowermgmt/config/config.json --secrets libs/autopowermgmt/config/secrets.json stop --device autopower22
```